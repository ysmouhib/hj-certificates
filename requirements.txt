# Only the SAT drivers in src/ need these. The certificate verifiers
# (verify_all.py at the repository root, and src/verify_certificates.py)
# use the Python standard library only and require nothing here.
python-sat>=1.8.dev13        # pysat; backends used by the rebuild scripts:
                             # cadical153, glucose42, minisat22; RC2 for MaxSAT
python-cryptosat>=5.11.0     # optional; used by src/hj1_3_interval.py
